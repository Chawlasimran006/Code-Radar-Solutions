#include<stdio.h>
int main()
{
    int N,i,j;
    scanf("%d",&N);
    for(i=1;i<=N;i++)
    {
        if(i%2==0)
        {
        for(j=0;j=i;j++)
        printf("%d",j);
        }
    }
}