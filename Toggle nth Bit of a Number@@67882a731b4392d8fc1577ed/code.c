#include <stdio.h>
int main()
{
    int a,n,num;
    scanf("%d%d",&a,&b);
    if(a<<n==1)
    {num=a & (1<<n);
    printf("%d",num);}
    else
    {num=0;
    printf("%d",num);
    }
    return 0;
}