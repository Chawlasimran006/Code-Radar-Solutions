#include <stdio.h>
int main()
{
    char str1[50],str2[50];//in the bracket it can be amy value but should be equal
    scanf("%s %s",&str1,&str2);
    printf("You entered: %s and %s",str1,str2);
    return 0;
}