#include<stdio.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(n<=1)
    printf("Not Prime");
    else
    {
        for(i=2;i<=i**(1/2);i++)
        {
            n%i==0;
            printf("Prime");
        }
    }
    
}